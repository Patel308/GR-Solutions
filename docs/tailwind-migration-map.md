# GR Solutions Tailwind Migration Map

`app/globals.css` remains as a visual fallback until sections are migrated and checked.

| Group | Old selectors/classes | Tailwind migration target |
| --- | --- | --- |
| Global/base | `html`, `body`, `a`, `img`, `input`, `textarea`, `select`, `button` | `font-main`, `text-textMain`, `bg-white`, `overflow-x-hidden`, `scroll-smooth`, `box-border`, `transition`, `focus-visible:*`; keep minimal reset CSS |
| Layout/base | `.container`, `.section`, headings, `.section-title`, `.primary-text`, `.text-muted`, `.mb-*` | `container`, `mx-auto`, `py-*`, `text-center`, `text-primary`, `text-textMuted`, `mb-*`, responsive grid utilities |
| Buttons | `.btn`, `.btn-primary`, `.btn-outline`, `.btn-accent`, `.hero-ctas`, `.nav-ctas`, `.btn-booking-submit`, `.btn-submit` | `inline-flex items-center justify-center gap-3 rounded-full px-* py-* font-extrabold shadow-cta transition duration-300 ease-old hover:-translate-y-*`; color variants use theme colors |
| Header/nav | `header`, `.navbar`, `.logo`, `.nav-logo`, `.nav-links`, `.active`, `.nav-ctas`, `.dropdown-menu`, `.mobile-menu-toggle`, `.mobile-active` | `sticky top-0 z-[1000] bg-white shadow-nav`, `container flex items-center justify-between`, `w-[150px] h-auto`, responsive `hidden lg:flex`, solid dropdown with `absolute bg-white rounded-2xl shadow-*` |
| Hero | `.hero`, `.hero-grid`, `.hero-content`, `.badge-group`, `.badge`, `.hero-image`, `.hero-image-badge`, `.hero-stats`, `.stat-value` | gradient background utilities, `grid lg:grid-cols-[1.2fr_0.8fr] gap-*`, clamp text, `inline-flex rounded-full bg-primary/10 text-primary`, `relative overflow-hidden rounded-[20px] shadow-oldLg` |
| Cards/grids | `.grid-2`, `.grid-3`, `.grid-4`, `.grid-2-alt`, `.card`, `.card-icon`, `.service-card-next`, `.service-card-detailed`, `.service-card-img-wrap`, `.service-card-benefits`, `.process-card`, `.core-value-card` | responsive `grid` utilities, `bg-white rounded-card p-10 border border-black/5 shadow-oldMd transition hover:-translate-y-* hover:shadow-oldLg`, image wrappers and benefit lists |
| Footer | `footer`, `.footer-grid`, `.footer-col`, `.footer-heading`, `.footer-heading-underline`, `.footer-brand-text`, `.footer-links`, `.footer-bottom`, `.footer-bottom-links-row`, `.contact-info`, `.social-links`, `.social-btn` | `bg-[#09091b] text-white py-*`, responsive grid columns, `text-white` headings, `text-[#b9c3d8]` links, social icon circle utilities; underline pseudo remains CSS if needed |
| FAQ | `.faq-container`, `.faq-list`, `.faq-item`, `.faq-question`, `.faq-answer`, active states | `grid gap-4`, `rounded-[18px] border border-primary/10 bg-white shadow-* overflow-hidden`, conditional active classes for blue header and expanded answer |
| Modal/form | `.modal`, `.modal.active`, `.modal-content`, `.close-modal`, `.modal-header`, `.enquiry-form`, `.input-container`, `.booking-form-card`, `.booking-field`, `.booking-select-wrap`, `.form-input-alt` | fixed overlay utilities, opacity/visibility state classes, `rounded-modal shadow-* max-h-* overflow-y-auto`, form field utilities |
| Contact page | `.contact-hero-pro`, `.contact-hero-grid-pro`, `.contact-hero-copy-pro`, `.contact-hero-image-pro`, `.contact-mid-pro`, `.contact-mid-grid-pro`, `.contact-row-pro`, `.appointment-card-pro` | gradient sections, responsive grid, clamp title, `rounded-3xl shadow-* object-cover`, row icon grid, appointment form card utilities |
| Blog | `.blog-grid-next`, `.blog-card-next`, `.featured-post-next`, `.read-more-next`, `.category-link` | `grid md:grid-cols-2 lg:grid-cols-3 gap-* items-stretch`, `flex flex-col overflow-hidden min-h-*`, `mt-auto font-black text-primary`, featured responsive grid |
| Carousel/testimonials | `.carousel-container`, `.carousel-track`, `.carousel-card`, `.specialty-carousel-next`, `.testimonial-section`, `.testimonial-container`, `.testimonial-track`, `.testimonial-slide`, `.testi-card-pro` | `animate-carousel`, `flex w-max gap-*`, card utility stacks; mask fade and complex track fallback can remain in CSS until verified |
| Floating buttons | `.floating-actions`, `.float-btn`, `.btn-whatsapp`, `.btn-call` | `fixed right-* bottom-* z-[900] flex flex-col gap-*`, `size-[54px] rounded-full flex items-center justify-center text-white shadow-oldLg`, WhatsApp/call colors |
| Animations | `scroll`, `carousel-loop`, `scrollTesti`, `pulse-green`, `soft-rise`, `.animate-on-scroll` | Tailwind keyframes `animate-carousel`, `animate-testimonials`, `animate-pulseGreen`, `animate-softRise`; reduced-motion and scroll-observer helpers can remain in CSS |
