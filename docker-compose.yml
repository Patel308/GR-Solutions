services:
  tv-wale:
    build:
      context: .
      args:
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL}
    container_name: tv-wale
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3022:3000"
